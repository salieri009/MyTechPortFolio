# ========================================
# DEFAULT PROFILE (Fallback)
# ========================================
# This file contains common configurations shared across all profiles
# Specific profile configurations are in application-{profile}.properties

# Server Configuration
server.port=8080
# Note: context-path removed to allow /api/v1 structure in controllers
# Frontend proxy handles /api -> http://localhost:8080/api/v1
server.servlet.context-path=

# MongoDB Configuration (Default)
spring.data.mongodb.uri=mongodb://localhost:27017/portfolio
spring.data.mongodb.database=portfolio
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.connection-pool.max-size=100
spring.data.mongodb.connection-pool.min-size=5

# API Documentation
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# CORS Configuration (Default)
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:5174
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
cors.allowed-headers=Content-Type,Authorization,X-Requested-With,X-Request-ID
cors.allow-credentials=false
cors.max-age=3600

# JWT Configuration (Default)
app.jwt.access-token-validity-in-ms=3600000
app.jwt.refresh-token-validity-in-ms=86400000

# 2FA Configuration
app.2fa.issuer=MyPortfolio

# Logging (Default)
logging.level.org.springframework.security=INFO
logging.level.org.springframework.data.mongodb.core.MongoTemplate=INFO
logging.level.com.mytechfolio.portfolio=INFO

# Actuator (Default)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Google OAuth Configuration
spring.security.oauth2.client.registration.google.client-id=1098017074065-i5kgtgj5upsvh06vtmhfi2ba78hh25sc.apps.googleusercontent.com

spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# GitHub OAuth Configuration
github.oauth.client-id=${GITHUB_CLIENT_ID:}
github.oauth.client-secret=${GITHUB_CLIENT_SECRET:}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:demo-jwt-secret-1234567890123456789012345678901234567890}

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email Application Settings
app.email.from=${EMAIL_FROM:noreply@salieri009.studio}
app.email.owner=${EMAIL_OWNER:owner@salieri009.studio}
app.email.enabled=${EMAIL_ENABLED:true}

# Performance Monitoring Settings
app.performance.slow-query-threshold-ms=${SLOW_QUERY_THRESHOLD_MS:1000}
app.performance.metrics-retention-count=${METRICS_RETENTION_COUNT:1000}


